export enum RoleEnum {
  OWNER = "owner",
  MANAGER = "manager",
  STAFF = "staff",
}

export enum DeptEnum {
  ADMIN = "admin",
  KITCHEN = "kitchen", 
  WAITER = "waiter", 
  BAR = "bar"
}

export enum StaffStatusEnum {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum PaymentStatusEnum {
  PENDING = "pending",
  PAID = "paid",
}